Start a working session on the Physical AI Agent project.

Do these in order:

1. Read `docs/PLAYBOOK.md` (reasoning patterns + working style — follow §11 and §12 exactly).
2. Read the "Current state" and most recent session-record sections of `CLAUDE.md`.
3. Run `git log --oneline -5` and `git status -sb`. If the branch is ahead of origin, tell Daniel immediately and remind him to push (project milestone rule).
4. List any items CLAUDE.md marks as unverified/pending (e.g. tests not yet run on his Mac, TODOs gated on "ask first").
5. Check whether today's goal matches a brief in `docs/briefs/` — if yes, read that brief before doing anything else.
6. Then ask Daniel what today's goal is (unless he already said), and clarify with questions until ~90% sure of intent.

Keep the summary of all this to a few lines — he prefers concise.
